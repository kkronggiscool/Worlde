<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Worlde</title>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
    <style>
        body {
            background: #121213;
            color: #fff;
            font-family: 'Roboto', Arial, sans-serif;
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            align-items: center;
            min-height: 100vh;
        }
        header {
            margin-top: 40px;
            font-size: 2.5rem;
            font-weight: 700;
            letter-spacing: 0.1em;
        }
        .subtitle {
            color: #818384;
            margin-bottom: 30px;
            font-size: 1.1rem;
        }
        .board {
            display: grid;
            grid-template-rows: repeat(6, 1fr);
            gap: 8px;
            margin-bottom: 30px;
        }
        .row {
            display: grid;
            grid-template-columns: repeat(5, 1fr);
            gap: 8px;
        }
        .cell {
            width: 50px;
            height: 50px;
            background: #121213;
            border: 2px solid #3a3a3c;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 2rem;
            font-weight: 700;
            text-transform: uppercase;
            color: #fff;
            border-radius: 4px;
        }
        .start-btn {
            background: #6aaa64;
            color: #fff;
            border: none;
            padding: 12px 32px;
            font-size: 1.2rem;
            font-weight: 700;
            border-radius: 6px;
            cursor: pointer;
            transition: background 0.2s;
        }
        .start-btn:hover {
            background: #538d4e;
        }
        footer {
            margin-top: auto;
            color: #818384;
            font-size: 0.95rem;
            padding: 20px 0;
        }
    </style>
</head>
<body>
    <header>Worlde</header>
    <div class="subtitle">A daily word puzzle inspired by Wordle</div>
    <div class="board">
        <% for(let i=0; i<6; i++) { %>
            <div class="row">
                <% for(let j=0; j<5; j++) { %>
                    <div class="cell"></div>
                <% } %>
            </div>
        <% } %>
    </div>
    <form action="/play" method="get">
        <button class="start-btn" type="submit">Start Game</button>
    </form>
    <footer>
        &copy; <%= new Date().getFullYear() %> Worlde. Not affiliated with NYT or Wordle.
    </footer>
</body>
</html>